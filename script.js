const operators = document.querySelectorAll(".buttons");
const numbers = document.querySelectorAll(".numberBtns");

// const [key, value] = numbers
// console.log(key, value)

// each button sends string to textarea input = operation = function?
// responsive to a keypad & keyboard numbers
